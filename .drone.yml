---
kind: pipeline
type: kubernetes
name: runner-hello

steps:
- name: hello
  image: alpine
  pull: if-not-exists
  commands:
    - echo "hello from k8s drone"
    - uname -a
    - echo "KEY=123" > .env.sh
- name: finish
  image: alpine
  commands:
    - source .env.sh
    - echo "finish"
    - echo $KEY

trigger:
  event:
    - push

---
kind: pipeline
type: kubernetes
name: go-hello

steps:
- name: test
  image: golang
  pull: if-not-exists
  volumes:
  - name: deps
    path: /go
  commands:
    - cd ./go/
    - echo "---- go test"
    - go test
    - ls -l /go
    - du -h -d1 /go
    - date >> /go/date.txt

- name: build
  image: golang
  pull: if-not-exists
  volumes:
  - name: deps
    path: /go
  commands:
    - cd ./go/
    - echo "---- go build"
    - go build
    - cat /go/date.txt
    - du -h -d1 /go
    - ls -l go*

volumes:
- name: deps
  temp: {}
